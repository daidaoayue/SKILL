% !Mode:: "TeX:UTF-8"
% !TEX program = xelatex
% Auto-wrapped by weekly-report-writer · scripts/render_pdf.py
% Academic style — modeled after buaathesis (宋体正文 / 黑体标题 / 无彩色装饰).
\documentclass[11pt,a4paper,UTF8]{ctexart}

% --- Geometry ---
\usepackage[a4paper,top=2.6cm,bottom=2.6cm,left=2.5cm,right=2.5cm,headheight=14pt]{geometry}

% --- Math ---
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}

% --- Tables: longtable (pandoc) + booktabs (academic rules) ---
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\usepackage{calc}
\usepackage{etoolbox}
% Center longtables on the page (\fill on both sides = stretchy equal margins).
\setlength\LTleft\fill
\setlength\LTright\fill
% Tighter, academic-style cells.
\AtBeginEnvironment{longtable}{\small\setlength{\tabcolsep}{6pt}\renewcommand{\arraystretch}{1.25}}
\AtBeginEnvironment{tabular}{\small\setlength{\tabcolsep}{6pt}\renewcommand{\arraystretch}{1.25}}

% --- Long file path breakability inside tables ---
% (\allowbreak{} injection happens in render_pdf.py — no extra package needed.)

% --- Verbatim blocks ---
\usepackage{fancyvrb}

% --- Hyperlinks (subtle, academic) ---
\usepackage[colorlinks=true,linkcolor=black,urlcolor=blue!50!black,citecolor=blue!50!black]{hyperref}

% --- Fonts: 宋体正文 + 黑体标题（学术中文标准搭配） ---
\usepackage{xeCJK}
\setCJKmainfont[BoldFont=SimHei,ItalicFont=KaiTi,AutoFakeBold=true]{SimSun}
\setCJKsansfont[AutoFakeBold=true]{SimHei}
\setCJKmonofont{SimHei}
% English fonts: keep TeX defaults (Latin Modern) — works with academic Chinese.

% --- Pandoc compatibility shims ---
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\pandocbounded}[1]{#1}

% --- Disable LaTeX auto-numbering for sections.
% Markdown headings already include manual numbering (e.g. "一、" / "6.3"），
% adding LaTeX's auto-numbering produces "6.3 6.3" / "三、3" doubles.
\setcounter{secnumdepth}{-1}

% --- Section formatting: 黑体加粗，无彩色，无装饰条 ---
\usepackage{titlesec}
\titleformat{\section}
  {\Large\bfseries\sffamily}
  {\thesection.}{0.6em}{}
\titleformat{\subsection}
  {\large\bfseries\sffamily}
  {\thesubsection}{0.6em}{}
\titleformat{\subsubsection}
  {\normalsize\bfseries\sffamily}
  {\thesubsubsection}{0.6em}{}
\titlespacing*{\section}{0pt}{1.6em}{0.8em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.5em}
\titlespacing*{\subsubsection}{0pt}{0.9em}{0.4em}

% --- Paragraph: 学术中文首行缩进 2 字符，行距 1.3 ---
\setlength{\parindent}{2em}
\setlength{\parskip}{0.4em plus 0.05em minus 0.05em}
\linespread{1.3}\selectfont

% --- Page header / footer (low-key academic) ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
\fancyhead[L]{\small\sffamily 周报 · weekly-report-writer}
\fancyhead[R]{\small\sffamily \thepage}

% --- Tighter list spacing ---
\usepackage{enumitem}
\setlist[itemize]{leftmargin=2em,itemsep=0.2em,topsep=0.3em,parsep=0pt}
\setlist[enumerate]{leftmargin=2em,itemsep=0.2em,topsep=0.3em,parsep=0pt}

% --- Slightly more flexible line-breaking for long content ---
\sloppy
\hfuzz=2pt

\begin{document}

% Body inserted here by render_pdf.py.
% =====================================================================
% BODY_PLACEHOLDER
% =====================================================================

\end{document}
